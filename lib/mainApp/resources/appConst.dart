import 'package:flutter/cupertino.dart';

class AppConst{
  static const DURATION = Duration(milliseconds: 500);
  static const DURATION_FAST = Duration(milliseconds: 200);

  static const PADDING = const EdgeInsets.symmetric(horizontal: 20,vertical: 12);
  static const PADDING_HORIZONTAL = const EdgeInsets.symmetric(horizontal: 20);
  static const PADDING_VERTICAL = const EdgeInsets.symmetric(vertical: 12);
}