import 'package:flutter/material.dart';

class NotificationTile extends StatelessWidget {
  NotificationTile({required this.image, required this.label, required this.time});
  final String image;
  final String label;
  final DateTime time;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
